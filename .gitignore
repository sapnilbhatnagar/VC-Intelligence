frontend/node_modules/
node_modules/
.env
frontend/.env
frontend/.env.local

# Python
__pycache__/
*.pyc

# Local SQLite database (recreated on startup, must not ship as seed data)
*.db

# Generated analysis outputs (existing samples kept; new runs ignored)
outputs/

# OS
.DS_Store
.vercel

# Local Python virtualenv + tooling (tests run via: uv venv .venv)
.venv/
.python-version
pyproject.toml
uv.lock

# Root-level lockfile (the real one is frontend/package-lock.json)
/package-lock.json

# Built frontend assets — Vercel rebuilds these from source on each deploy
frontend/dist/
